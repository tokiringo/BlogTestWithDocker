import React from 'react';
import styles from "../css/Header.module.css";

export const Header = ()=> {
    return (
        <div className={styles.Header}>
            <h1>「私カレンダー」</h1>
        </div>
    )
}